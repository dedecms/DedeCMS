$(document).ready(function () {
    $.get('/plus/user_data.php?dopost=status', function () {
    }).done(function (data) {
        $("#dede-topbar").append(data);
    })
    .fail(function () {
    alert("请确保/plus/user_data.php文件存在!");
    });

    $.get('/plus/user_data.php?dopost=cart', function () {
    }).done(function (data) {
        $("#cart").after(data);
    })
    .fail(function () {
    alert("请确保/plus/user_data.php文件存在!");
    });

    $.get('/plus/user_data.php?dopost=orders', function () {
    }).done(function (data) {
        $("#orders").append(data);
    })
    .fail(function () {
    alert("请确保/plus/user_data.php文件存在!");
    });


    // checkbox 全选或选取消
$("#select-all").click(function (event) {
    if (this.checked) {
    $(":checkbox").prop("checked", true);
    } else {
    $(":checkbox").prop("checked", false);
    }
    });



});



// 多选通用动作
function DedeCMS_JSCODE_Version_Checked_Action(url = "") {
var _CheckedVals = _DedeCMS_JSCODE_Version_Get_Checkbox_Checked()
if (_CheckedVals == "") {
alert("必须选择一个或多个文档！");
} else {
location = url+_CheckedVals
}
}
// 多选通用动作
function _DedeCMS_JSCODE_Version_Get_Checkbox_Checked() {
return $(".dede-checkbox:checkbox:checked").map(function () {
return this.value;
}).get().join("|");
}
